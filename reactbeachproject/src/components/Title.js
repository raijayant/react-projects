import React from 'react'

export default function Title ({ title }) {
  return (
    <div className='section-title'>
      <h4>{title}</h4>
      <div />
    </div>
  )
}

// creating a title component so that
// we can use it anywhere in the application
